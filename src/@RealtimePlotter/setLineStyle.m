function setLineStyle( obj,lineStyle,marker,markerFaceColor,visible,selected )
% Reset the animated line style

obj.animLine.lineStyle = lineStyle;
obj.animLine.marker = marker;
obj.animLine.markerFaceColor = markerFaceColor;
obj.animLine.visible = visible;
obj.animLine.selected = selected;

end
